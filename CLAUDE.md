# TMSviewer — Claude Code Instructions

## Font size defaults
- Default UI font size: **11pt**
- Explicitly set fonts on all widgets instead of relying on OS defaults
- Any new UI element should use at least 11pt unless there is a layout constraint
